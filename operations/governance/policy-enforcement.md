# Policy enforcement

Policy enforcement converts organizational rules into deterministic checks around agent execution. Policies can constrain tools, resources, models, data locations, spending, side effects, and required approvals.

Evaluate policy before the consequential action. Asking the model to remember a policy is useful context but not enforcement.
