# Supervisor pattern

A supervisor agent coordinates work performed by tools or specialist agents. It interprets the objective, selects delegates, integrates results, and decides whether additional work is required.

Supervision is useful when tasks require heterogeneous capabilities, but it adds model turns and coordination overhead. Keep delegated objectives and permissions bounded.
