# Router pattern

A router classifies an input or task and sends it to the appropriate model, workflow, agent, tool set, or knowledge source.

Routing can be deterministic, model-based, or hybrid. Evaluate routing accuracy separately from the quality of the destination systems.
