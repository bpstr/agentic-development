# Structured web data

Structured web data expresses entities, properties, relationships, and actions in machine-readable form alongside or instead of rendered content. JSON-LD and Schema.org vocabularies are common web examples.

For agents, structured data can reduce ambiguity during discovery, but transactional state should still be verified against authoritative services before consequential actions.
