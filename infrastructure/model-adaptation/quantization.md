# Model quantization

Quantization stores or computes model weights and activations at reduced numerical precision to lower memory use and potentially improve inference efficiency.

The effect depends on model architecture, quantization method, hardware, runtime, and workload. Evaluate the actual quantized artifact because quality and performance can differ from the full-precision model.
