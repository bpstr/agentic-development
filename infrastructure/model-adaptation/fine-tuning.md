# Fine-tuning

Fine-tuning continues training a pretrained model on task-specific examples or preferences. It can improve consistency, style, formatting, classification, extraction, and specialized behavior when representative training data exists.

Fine-tuning should be evaluated against a strong prompted baseline. Maintain separate training and evaluation data and record the base model, training configuration, dataset revision, and resulting model version.
