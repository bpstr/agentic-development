# Model distillation

Distillation transfers behavior from a stronger teacher model or system into a smaller or cheaper student model using generated labels, traces, or training targets.

It is useful when a stable task can be represented by a dataset and the production workload benefits from lower latency or cost. Distillation reproduces observed behavior; it does not automatically transfer every capability of the teacher.
