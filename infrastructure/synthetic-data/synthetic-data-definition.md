# Synthetic data

Synthetic data is generated rather than directly collected from real-world observations. Models can create prompts, conversations, labels, tool traces, edge cases, and simulated user behavior for training or evaluation.

Synthetic data can increase coverage but can also reproduce the generator's biases and errors. Mix generation with filtering, diversity checks, and human or authoritative validation where correctness matters.
