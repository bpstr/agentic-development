# Agent spending limits

Spending limits constrain autonomous purchasing or paid tool use. Limits can apply per transaction, task, merchant, category, time window, or total budget.

Enforce limits outside the model. The model can reason about a budget, but the payment or orchestration layer must reject operations that exceed it.
