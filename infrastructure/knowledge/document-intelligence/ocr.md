# Optical character recognition

Optical character recognition converts text represented as pixels into machine-readable characters. OCR is useful for scans and images but introduces recognition errors and often loses document structure.

Prefer native text extraction when a document already contains a reliable text layer. Use OCR when the content is genuinely image-based or the embedded text is unusable.
