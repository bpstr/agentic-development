# Table extraction

Table extraction reconstructs rows, columns, headers, merged cells, and values from structured documents or visual layouts. Plain text extraction can destroy these relationships.

Validate numerical and categorical data against the source layout when downstream calculations or decisions depend on it.
