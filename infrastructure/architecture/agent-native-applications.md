# Agent-native applications

An agent-native application treats model-driven work as a first-class execution mode rather than adding chat to an otherwise unchanged product. Domain operations remain explicit application capabilities, while agents decide when and how to combine them.

The architecture should preserve deterministic boundaries around identity, authorization, transactions, durable state, and side effects.
