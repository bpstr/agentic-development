# Deterministic boundaries

A deterministic boundary places critical behavior in ordinary software rather than probabilistic model output. Identity resolution, authorization, accounting, transactions, idempotency, schema validation, and policy enforcement are common examples.

Models can choose among permitted operations; deterministic code decides whether those operations are valid and commits their effects.
