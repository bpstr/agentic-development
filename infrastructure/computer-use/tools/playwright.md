# Playwright

Official documentation: https://playwright.dev/

Playwright is browser automation software rather than an AI agent framework. Agent systems can use it as a deterministic browser-control layer for navigation, DOM interaction, screenshots, downloads, uploads, and browser-context management.

Prefer semantic locators and application state over coordinate clicks when the page structure is available.
