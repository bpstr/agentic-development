# OAuth for agents

OAuth can authorize agent-accessible services without sharing a user's password. Agent systems commonly use authorization-code flows for user delegation and client credentials or workload identities for service-to-service access.

Scopes should correspond to real capabilities. Tool descriptions are not authorization boundaries; the resource server must enforce the granted scope.
