# Agent identity

Agent identity describes how a system identifies the software agent, service, user, organization, and delegated authority involved in an operation. These identities should remain distinguishable in logs and authorization decisions.

An agent acting for a user is not the user. Record both the initiating principal and the executing agent where possible.
