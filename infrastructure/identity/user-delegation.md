# User delegation

User delegation allows an agent to perform bounded actions on behalf of a user. Delegation should specify scope, duration, target resources, and revocation behavior rather than transferring unrestricted credentials.

The receiving service should be able to distinguish delegated authority from direct user interaction.
