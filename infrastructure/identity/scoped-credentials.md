# Scoped credentials

Scoped credentials restrict an agent to the minimum resources and operations required for its task. Useful restrictions include tenant, repository, project, API operation, environment, spending limit, and expiration time.

Short-lived credentials reduce exposure when an agent environment or tool invocation is compromised.
