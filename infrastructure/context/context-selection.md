# Context selection

Context selection chooses which available information should enter the next model request. Selection can use recency, explicit references, retrieval scores, task state, entity relationships, user corrections, or deterministic application rules.

Selection should favor authoritative current state over stale summaries when the two conflict.
