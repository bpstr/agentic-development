# Context compaction

Context compaction reduces accumulated interaction state into a smaller representation that preserves decisions, unresolved work, identifiers, and other information needed later.

Compaction is lossy. Preserve stable references to authoritative records so important details can be reloaded instead of relying exclusively on generated summaries.
