# Instruction hierarchy

Agent systems commonly combine instructions from several scopes: platform, application, organization, project, user, task, and reusable skill. An instruction hierarchy defines precedence when these sources conflict.

Authorization should not be encoded only as instructions. The application must enforce permissions independently of model compliance.
