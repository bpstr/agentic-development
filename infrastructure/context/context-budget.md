# Context budgets

A context budget allocates limited inference capacity among instructions, user input, conversation history, retrieved knowledge, tool schemas, tool results, and expected output.

Budgeting makes context an explicit system resource. Applications can reserve space for required instructions and output, then dynamically select evidence and history for the remaining capacity.
