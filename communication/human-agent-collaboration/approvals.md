# Agent approvals

An approval authorizes a specific pending action, plan, or transition. Store the proposed operation, relevant arguments, approving principal, time, scope, and resulting execution receipt.

Do not treat conversational phrases remembered by the model as durable authorization. Approval belongs in application state.
